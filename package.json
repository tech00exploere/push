{
  "name": "commitpost",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix blog_backend",
    "dev:frontend": "npm run dev --prefix blogging_frontend",
    "build:frontend": "npm run build --prefix blogging_frontend",
    "start:backend": "npm run start --prefix blog_backend"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  }
}
